

#include <iostream>
using namespace std;

// Broblem1
/*
struct strNumbers {
	int A, B,swp;
};
strNumbers RedNumbers(){

	strNumbers Numbers;
	cout << "Donner A : " << endl;
	cin >> Numbers.A;
	cout << "Donner B : " << endl;
	cin >> Numbers.B;

	return Numbers;
}
bool isPositive(strNumbers Numbers){
	return (Numbers.A > 0 && Numbers.B > 0) || (Numbers.A < 0 && Numbers.B < 0);
}
strNumbers SwapNubers(strNumbers Numbers) {

	Numbers.swp = Numbers.A;
	Numbers.A = Numbers.B;
	Numbers.B = Numbers.swp;

	cout << "Swap de nomber A et B : " << endl;
	cout << " A = " << Numbers.A << endl;
	cout << " B = " << Numbers.B << endl;

	return Numbers;
}
void PrintMethods(strNumbers Numbers){

	if (isPositive(Numbers)){
		SwapNubers(Numbers);
	}
	else {
		Numbers.swp = Numbers.A;
		Numbers.A = Numbers.A + Numbers.B;
		Numbers.B = Numbers.B * Numbers.swp;
		cout << " A = A + B = " << Numbers.A << endl;
		cout << " B = A * B = " << Numbers.B << endl;
	}
}

int main()
{
	PrintMethods(RedNumbers());
}
*/

int main()
{
	
}
